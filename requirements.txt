# Core dependencies for Lahore Temperature Prediction Pipeline

# Airflow
apache-airflow>=2.7.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# API requests
requests>=2.31.0

# Data profiling (for Step 2.2)
# Use ydata-profiling (newer version, recommended)
ydata-profiling>=4.0.0
# OR pandas-profiling (legacy, if ydata-profiling doesn't work)
# pandas-profiling>=3.0.0

# For Parquet file support
pyarrow>=10.0.0

# MLflow (for experiment tracking - Phase II Step 4)
mlflow>=2.8.0

# DVC (for data versioning - Step 3)
dvc[s3]>=3.0.0

# Machine Learning (Phase II Step 4)
scikit-learn>=1.3.0
joblib>=1.3.0

# Cloud Storage (Step 2.3)
boto3>=1.28.0
botocore>=1.31.0

# FastAPI (for model serving)
fastapi>=0.104.0
uvicorn>=0.24.0

# Monitoring
prometheus-client>=0.19.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

